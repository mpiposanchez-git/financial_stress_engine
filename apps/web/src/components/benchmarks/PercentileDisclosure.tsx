export function PercentileDisclosure() {
  return (
    <section className="result-card" aria-label="Percentile ranking disclosure">
      <h2>Percentile ranking disclosure</h2>
      <p>
        Approximate benchmark only. Percentile ranking uses the UK HBAI BHC definition and depends on year and
        definitions.
      </p>
      <p>It is not advice and not a measure of worth.</p>
    </section>
  );
}
